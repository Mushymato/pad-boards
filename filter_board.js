var orb_list = ["R", "B", "G", "L", "D"];
function minOrbFilter(orb, minimum){
	$("data-ratios").each
}
function addMinOrbListeners(){
	$(".orb-count > input[type^=range]").each(function(index){
		console.log($(this));
	});
}